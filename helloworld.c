#include <stdio.h>
/* Loads stdio library. To make this executable, must compile in terminal by using  gcc -o helloworld -ansi helloworld.c */
main() {
/* printf method used to output on screen and \n is for newline character */
	printf("Hello World!\n");
/* Must return something but since nothing to return use 0 */
	return(0);
}
